# 🚀 开发环境使用指南

## 快速开始

### 方式 1：一键启动（推荐）
```bash
# 快速启动，自动处理常见问题
pnpm start
# 或直接运行
./scripts/quick-start.bat
```

### 方式 2：智能启动脚本
```bash
# 正常启动
pnpm dev

# 清理环境后启动（解决端口冲突）
pnpm run dev:clean

# 仅检查环境状态
pnpm run dev:check
```

### 方式 3：PowerShell 完整控制
```powershell
# 基本启动
./scripts/dev.ps1

# 清理后启动
./scripts/dev.ps1 -Clean

# 构建模式
./scripts/dev.ps1 -Build

# 强制终止所有进程
./scripts/dev.ps1 -Kill

# 检查环境状态
./scripts/dev.ps1 -Check
```

## 🔧 故障排除

### 端口占用问题
```bash
# 方法1：使用清理脚本
pnpm run dev:clean

# 方法2：手动清理（Windows）
netstat -ano | findstr ":1420"
taskkill /pid [PID] /f
```

### 依赖问题
```bash
# 重新安装依赖
rm -rf node_modules
pnpm install

# 清理 Rust 缓存
cd src-tauri
cargo clean
```

### Rust 环境问题
```bash
# 检查 Rust 安装
rustc --version
cargo --version

# 更新 Rust
rustup update

# 添加 Windows 目标平台
rustup target add x86_64-pc-windows-msvc
```

## 📋 开发命令参考

### 开发相关
- `pnpm start` - 快速启动
- `pnpm dev` - 正常开发模式  
- `pnpm run dev:clean` - 清理后启动
- `pnpm run dev:check` - 环境检查

### 构建相关
- `pnpm build` - 构建生产版本
- `pnpm preview` - 预览构建结果

### 代码质量
- `pnpm lint` - 代码检查
- `pnpm lint:fix` - 自动修复
- `pnpm format` - 代码格式化
- `pnpm type-check` - 类型检查

### 测试相关
- `pnpm test` - 运行测试
- `pnpm test:ui` - 测试界面
- `pnpm test:coverage` - 覆盖率报告

## 🌟 脚本功能特性

### ✅ 智能环境检查
- 自动检测 Node.js、pnpm、Rust 版本
- 端口占用检测和自动清理
- 依赖完整性验证

### ✅ 优雅进程管理
- 优先尝试正常关闭进程
- 超时后自动强制终止
- 避免僵尸进程

### ✅ 自动故障修复
- 端口冲突自动处理
- 依赖缺失自动安装
- 环境变量自动配置

### ✅ 开发者友好
- 彩色输出和进度提示
- 详细的错误诊断信息
- 多种启动方式选择

## 🔍 常见问题

### Q: 启动时提示端口被占用？
A: 使用 `pnpm run dev:clean` 清理后重启

### Q: Rust 编译失败？
A: 检查是否安装了 Visual Studio Build Tools 或 MinGW

### Q: 依赖安装失败？
A: 尝试删除 node_modules 后重新安装

### Q: 界面无法加载？
A: 检查防火墙设置，确保允许本地端口访问

## 📖 开发流程建议

1. **首次启动**：使用 `pnpm start` 快速开始
2. **日常开发**：使用 `pnpm dev` 正常启动  
3. **遇到问题**：使用 `pnpm run dev:clean` 清理重启
4. **发布前**：运行 `pnpm build` 构建测试

---

💡 **提示**: 所有脚本都支持 `--help` 参数查看详细用法